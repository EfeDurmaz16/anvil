# Anvil environment configuration
# Copy this file to `.env` and adjust values for your environment.
#
# Notes:
# - Config file: `configs/anvil.yaml` is loaded first.
# - Environment variables override config keys via Viper (`ANVIL_` prefix, dots become underscores).

# ------------------------------------------------------------------------------
# LLM (optional)
# ------------------------------------------------------------------------------
# Set to `none` for air‑gapped / template-only mode (recommended default for on‑prem pilots).
# Supported: anthropic, openai, groq, huggingface, ollama, together, deepseek, custom, none
ANVIL_LLM_PROVIDER=groq

# Provider-specific model name (required when provider != none)
ANVIL_LLM_MODEL=qwen/qwen3-32b

# API key (required for most cloud providers; often empty for local endpoints)
ANVIL_LLM_API_KEY=your-api-key-here

# OpenAI-compatible base URL override (for on‑prem / customer VPC endpoints)
# Examples:
# - Ollama: http://localhost:11434/v1
# - vLLM:   http://localhost:8000/v1
ANVIL_LLM_BASE_URL=https://api.groq.com/openai/v1

# ------------------------------------------------------------------------------
# Temporal (required for workflow mode / worker)
# ------------------------------------------------------------------------------
ANVIL_TEMPORAL_HOST=localhost:7233
ANVIL_TEMPORAL_NAMESPACE=default
ANVIL_TEMPORAL_TASK_QUEUE=anvil-tasks

# ------------------------------------------------------------------------------
# Security
# ------------------------------------------------------------------------------
ANVIL_API_KEY=
ANVIL_CORS_ORIGINS=http://localhost:3000,http://localhost:9090

# ------------------------------------------------------------------------------
# Graph DB (optional - Neo4j)
# ------------------------------------------------------------------------------
ANVIL_GRAPH_URI=
ANVIL_GRAPH_USERNAME=
ANVIL_GRAPH_PASSWORD=

# Neo4j convenience aliases (used by docker-compose)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASS=anvilpass

# ------------------------------------------------------------------------------
# Vector DB (optional - Qdrant)
# ------------------------------------------------------------------------------
ANVIL_VECTOR_HOST=
ANVIL_VECTOR_PORT=
ANVIL_VECTOR_COLLECTION=anvil

# Qdrant convenience aliases (used by docker-compose)
QDRANT_HOST=localhost
QDRANT_PORT=6334

# ------------------------------------------------------------------------------
# Logging
# ------------------------------------------------------------------------------
ANVIL_LOG_LEVEL=info
ANVIL_LOG_FORMAT=json

# ------------------------------------------------------------------------------
# Observability (optional)
# ------------------------------------------------------------------------------
ANVIL_OTLP_ENDPOINT=
ANVIL_AUDIT_LOG=audit.jsonl
